# Schéma API Render / PostgreSQL

- **`schema.sql`** — tables pour `sqcdp-api` (Express + JWT optionnel Supabase)
- **RLS multi-tenant Supabase** → voir `../supabase/migrations/` (notamment `003_site_members_rls.sql`)
